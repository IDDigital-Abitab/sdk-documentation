name: Publish Documentation

on:
  push:
    branches:
      - main

jobs:
  preprod-deploy:
    name: Deploy Documentation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: /
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20.x"
      - name: Cache Node.js modules
        uses: actions/cache@v4
        with:
          path: /node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node20-
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Build
        run: yarn docs:build
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v1
      #   with:
      #     aws-access-key-id: ${{ secrets.ABITAB_ECR_AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.ABITAB_ECR_AWS_SECRET_ACCESS_KEY }}
      #     aws-region: eu-west-1

      # - name: Copy files to the website with the AWS CLI
      #   run: |
      #     aws s3 cp --recursive --cache-control 600 build/ s3://id-digital-2.0-documentation --exclude "*.DS_Store" --exclude "index.html" --exclude "sw.js" --exclude "sw.js.gz"
      #     aws s3 cp --recursive --cache-control max-age=0,must-revalidate,public build/ s3://id-digital-2.0-documentation --exclude "*" --include "index.html" --include "sw.js" --include "sw.js.gz"
